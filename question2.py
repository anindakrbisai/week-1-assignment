arr = [12, 35, 7, 64, 22]

# Using built-in function
max_element = max(arr)

# Or manually
# max_element = arr[0]
# for num in arr[1:]:
#     if num > max_element:
#         max_element = num

print("The largest element is:", max_element)
